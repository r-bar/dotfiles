[bootstrap.packages]
# Not currently included mise packages
# | `alchemmist/tap/lazy-tmux` | Not in official repos — only exists as an AUR package (`lazy-tmux`), which plain `pacman` can't install |
# | `claude-cmd` | No Arch or AUR package found at all |
# | `pi-coding-agent` | AUR-only (several competing packages: `pi-coding-agent`, `-bin`, `-git`), nothing official |
"brew:alchemmist/tap/lazy-tmux"   = { os = "macos" }
"brew:bat"                        = { os = "macos" }
"brew:btop"                       = { os = "macos" }
"brew:chezmoi"                    = { os = "macos" }
"brew:claude-cmd"                 = { os = "macos" }
"brew:colima"                     = { os = "macos" }
"brew:coreutils"                  = { os = "macos" }
"brew:docker"                     = { os = "macos" }
"brew:docker-buildx"              = { os = "macos" }
"brew:docker-compose"             = { os = "macos" }
"brew:eza"                        = { os = "macos" }
"brew:fd"                         = { os = "macos" }
"brew:fish"                       = { os = "macos" }
"brew:fzf"                        = { os = "macos" }
"brew:gawk"                       = { os = "macos" }
"brew:git-delta"                  = { os = "macos" }
"brew:gnu-getopt"                 = { os = "macos" }
"brew:gnu-sed"                    = { os = "macos" }
"brew:gnu-tar"                    = { os = "macos" }
"brew:gnu-which"                  = { os = "macos" }
"brew:gnupg"                      = { os = "macos" }
"brew:go"                         = { os = "macos" }
"brew:grep"                       = { os = "macos" }
"brew:kubernetes-cli"             = { os = "macos" }
"brew:kubectx"                    = { os = "macos" }
"brew:kubelogin"                  = { os = "macos" }
"brew:make"                       = { os = "macos" }
"brew:mise"                       = { os = "macos" }
"brew:neovim"                     = { os = "macos" }
"brew:pi-coding-agent"            = { os = "macos" }
"brew:pnpm"                       = { os = "macos" }
"brew:postgresql@16"              = { os = "macos" }
"brew:qemu"                       = { os = "macos" }
"brew:rbw"                        = { os = "macos" }
"brew:reattach-to-user-namespace" = { os = "macos" }
"brew:ripgrep"                    = { os = "macos" }
"brew:rustup"                     = { os = "macos" }
"brew:temporalio/brew/tcld"       = { os = "macos" }
"brew:tmux"                       = { os = "macos" }
"brew:tree-sitter-cli"            = { os = "macos" }
"brew:uv"                         = { os = "macos" }
"brew:watch"                      = { os = "macos" }
"brew:xh"                         = { os = "macos" }
"brew:yq"                         = { os = "macos" }
"pacman:bat"                      = { os = "linux" }
"pacman:btop"                     = { os = "linux" }
"pacman:chezmoi"                  = { os = "linux" }
"pacman:docker"                   = { os = "linux" }
"pacman:docker-buildx"            = { os = "linux" }
"pacman:docker-compose"           = { os = "linux" }
"pacman:eza"                      = { os = "linux" }
"pacman:fd"                       = { os = "linux" }
"pacman:fish"                     = { os = "linux" }
"pacman:fzf"                      = { os = "linux" }
"pacman:gawk"                     = { os = "linux" }
"pacman:git-delta"                = { os = "linux" }
"pacman:gnupg"                    = { os = "linux" }
"pacman:go"                       = { os = "linux" }
"pacman:go-yq"                    = { os = "linux" }
"pacman:make"                     = { os = "linux" }
"pacman:mise"                     = { os = "linux" }
"pacman:neovim"                   = { os = "linux" }
"pacman:opencode"                 = { os = "linux" }
"pacman:pnpm"                     = { os = "linux" }
"pacman:qemu-full"                = { os = "linux" }
"pacman:rbw"                      = { os = "linux" }
"pacman:ripgrep"                  = { os = "linux" }
"pacman:rustup"                   = { os = "linux" }
"pacman:tmux"                     = { os = "linux" }
"pacman:tree-sitter-cli"          = { os = "linux" }
"pacman:uv"                       = { os = "linux" }
"pacman:xh"                       = { os = "linux" }

[tools]
stylua = "2"

[tasks.nvim-lazy-lock-check]
description = "Ensure nvim's lazy-lock.json is in sync"
run = """
LOCKFILE_PATH=dot_config/nvim/lazy-lock.json
if [ ! -f $LOCKFILE_PATH ]; then
  echo "Cannot find chezmoi's neovim lockfile at $LOCKFILE_PATH"
  exit 1
fi

diff $LOCKFILE_PATH $(chezmoi target-path $LOCKFILE_PATH)
RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo
  echo "Neovim's plugin lockfile is out of sync with the state in chezmoi"
fi
exit $RESULT
"""

[tasks.nvim-spellfile-check]
description = "Ensure nvim's spellfile is in sync"
run = """
SPELLFILE=dot_local/share/nvim/site/spell/en.utf-8.add
if [ ! -f $SPELLFILE ]; then
  echo "Cannot find chezmoi's neovim spellfile at $SPELLFILE"
  exit 1
fi

diff $SPELLFILE $(chezmoi target-path $SPELLFILE)
RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo
  echo "Neovim's local spellfile is out of sync with the state in chezmoi"
fi
exit $RESULT
"""

[tasks.nvim-spellfile-update]
description = "Update nvim's spellfile to match the current state in use on this machine"
run = "chezmoi re-add ~/.local/share/nvim/site/spell/en.utf-8.add"

[tasks.nvim-lazy-lock-update]
description = "Update nvim's lazy-lock.json to match the current state in use on this machine"
run = "chezmoi re-add ~/.config/nvim/lazy-lock.json"

[tasks.format-lua]
description = "Format Lua files using stylua"
usage = """
arg "[files]" help="The file(s) to format" var=#true
"""
run = "stylua ${usage_files:-dot_config/nvim dot_hammerspoon}"
